# SPDX-License-Identifier: MPL-2.0
# This workflow is managed by gh actions-lock.
name: Central Estate CI/CD Audit

# The called reusable declares `permissions: contents: read`; a caller must grant
# at least what the reusable declares, and this grants exactly that — no more.
permissions:
  contents: read

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  workflow_call:

jobs:
  call-estate-audit:
    uses: hyperpolymath/cicd-suite/.github/workflows/main-estate-audit.yml@222b1d950f7161022ed968868422fe0ac934f74a  # cicd-suite MAIN (merged #18 = the remote-form FULL-SHA + de-onboarded callee). Pin MUST be a commit reachable from a branch: the b15202a pin worked while #18's branch lived (run 35282081912 spawned) but went 0-job once the merge deleted it (run 35282493679) — PR-head commits are only pull-ref reachable. Bare callee checkout fetches THIS repo: the 26 gates audit this estate. Repin on callee updates.
