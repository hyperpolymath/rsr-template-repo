# SPDX-License-Identifier: MPL-2.0
# This workflow is managed by gh actions-lock.
name: Central Estate CI/CD Audit

# The called reusable declares `permissions: contents: read`; a caller must grant
# at least what the reusable declares, and this grants exactly that — no more.
permissions:
  contents: read

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  workflow_call:

jobs:
  call-estate-audit:
    uses: hyperpolymath/cicd-suite/.github/workflows/main-estate-audit.yml@ecd0240d80881c9b85b6f1aff0d056dd5a076a54  # cicd-suite main: cross-repo-compatible callee (./actions/* resolves against the callee checkout at this ref; de-onboarded from actions.lock — local-path actions are a gh-actions-lock short-term gap; manifest-check internals SHA-pinned per sha_pinning_required). Repin on callee updates.
