# SPDX-License-Identifier: MPL-2.0
# This workflow is managed by gh actions-lock.
name: Central Estate CI/CD Audit

# The called reusable declares `permissions: contents: read`; a caller must grant
# at least what the reusable declares, and this grants exactly that — no more.
permissions:
  contents: read

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  workflow_call:

jobs:
  call-estate-audit:
    uses: hyperpolymath/cicd-suite/.github/workflows/main-estate-audit.yml@af7062381f7ae5f731c12051a671a373c72d7541  # cicd-suite main: REMOTE-FORM callee (hyperpolymath/cicd-suite/actions/*@main resolved by API + repo-level lock key, tool-written with transitive deed/k9 SHA keys — works locally AND cross-repo; ./ resolves against the CALLER repo, $/ against the CALLER commit — both proven dead cross-repo). The callee's bare checkout fetches THIS repo, so the 26 gates audit this estate. Repin on callee updates.
