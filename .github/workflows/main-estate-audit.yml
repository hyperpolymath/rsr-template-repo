# SPDX-License-Identifier: MPL-2.0
# This workflow is managed by gh actions-lock.
name: Central Estate CI/CD Audit

# The called reusable declares `permissions: contents: read`; a caller must grant
# at least what the reusable declares, and this grants exactly that — no more.
permissions:
  contents: read

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  workflow_call:

jobs:
  call-estate-audit:
    uses: hyperpolymath/cicd-suite/.github/workflows/main-estate-audit.yml@7689b046fe7c85365657193edf937307960fd4ec  # repinned 2026-09-17 (2/2): dbe2b2a content still carried 26 @main action refs that sha_pinning_required rejects at startup (0-job runs); 7689b04 = cicd-suite#11, all refs SHA-pinned
