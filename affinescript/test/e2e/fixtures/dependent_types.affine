// SPDX-License-Identifier: PMPL-1.0-or-later
// End-to-end test: dependent-type surface syntax (parse-only)
// Tests: type aliases and generic functions with where-clause constraints
// Note: refinement predicates are parsed but not yet reduced/checked.

type nat = Int;

fn succ(n: nat) -> nat = n + 1;

fn add_nats(a: nat, b: nat) -> nat = a + b;

fn zero() -> nat = 0;
