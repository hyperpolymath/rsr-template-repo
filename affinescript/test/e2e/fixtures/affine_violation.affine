// SPDX-License-Identifier: PMPL-1.0-or-later
// End-to-end test: function with double variable use
// The quantity checker should flag this for linear-annotated vars.
// Since quantity annotations aren't in the lexer, we test the quantity
// checker programmatically.

fn double_use(x: Int) -> Int = x + x;
