// Test trait declarations
trait Eq {
  fn eq(ref self, ref other: Self) -> Bool;
}

trait Ord: Eq {
  fn cmp(ref self, ref other: Self) -> Ordering;
}

impl Eq for Int {
  fn eq(ref self, ref other: Int) -> Bool {
    true
  }
}
