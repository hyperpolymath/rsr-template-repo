// SPDX-License-Identifier: PMPL-1.0-or-later
// Record/row building blocks (parser-compatible example)

struct Point2D {
  x: Int,
  y: Int
}

struct Point3D {
  x: Int,
  y: Int,
  z: Int
}

fn getX(p: Point2D) -> Int = p.x;

fn mk_point(x: Int, y: Int) -> Point2D {
  { x: x, y: y }
}
