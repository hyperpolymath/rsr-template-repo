// SPDX-License-Identifier: PMPL-1.0-or-later
// SPDX-FileCopyrightText: 2025 hyperpolymath

// Test module imports

use Core::{min, max};
use Math::{pow, gcd};

fn main() -> Int {
  let a = min(10, 20);
  let b = max(10, 20);
  let c = pow(2, 3);
  let d = gcd(48, 18);
  return a + b + c + d;  // 10 + 20 + 8 + 6 = 44
}
