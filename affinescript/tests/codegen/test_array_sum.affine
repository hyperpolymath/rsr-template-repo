// Test array with sum of elements

fn main() -> Int {
  let arr = [5, 10, 15, 20, 25];

  // Manually sum elements (no loops yet in codegen)
  let sum = arr[0] + arr[1] + arr[2] + arr[3] + arr[4];

  return sum;  // Should return 75
}
