// Test string equality via pointer comparison

fn main() -> Int {
  let s1 = "Hello";
  let s2 = "Hello";

  // If deduplication works, s1 and s2 have the same pointer value
  // Compare them: if equal, both point to 2048
  return s1 - s2;  // Should return 0 if equal
}
