// Test array bounds - first and last elements

fn main() -> Int {
  let arr = [100, 200, 300, 400, 500];

  let first = arr[0];   // 100
  let last = arr[4];    // 500

  return first + last;  // Should return 600
}
