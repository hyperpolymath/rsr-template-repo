// Test closure capturing multiple variables

fn main() -> Int {
  let x = 10;
  let y = 20;
  let z = 30;

  let f = |a: Int| -> Int {
    return x + y + z + a;
  };

  return f(5);  // Should return 65 (10+20+30+5)
}
