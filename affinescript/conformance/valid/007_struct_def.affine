// Conformance test: struct definitions

type Point = {
  x: Int,
  y: Int
}

type Person = {
  name: String,
  age: Int,
  email: String
}

type Generic[T] = {
  value: T,
  count: Int
}

fn make_point(x: Int, y: Int) -> Point {
  Point { x: x, y: y }
}
